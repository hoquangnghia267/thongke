<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Công Ty CP Chữ Ký Số Vi Na</title>
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="min-h-screen flex flex-col bg-gray-100">

  <!-- Header -->
  <%- include('../partials/header') %>
  <%- include('../partials/sidebar') %>
  <!-- Main content -->
  <main class="flex-grow p-6 md:p-10 md:ml-64 mt-20"> 
    <div class="max-w-2xl mx-auto bg-white p-6 rounded shadow">
      <h1 class="text-2xl font-bold mb-4 text-center">Thống kê tổng số lượng từ ngày đến ngày</h1>

      <form method="POST" action="/thongke/ngay" class="grid grid-cols-1 md:grid-cols-2 gap-4 mb-6">
        <input type="text" name="serialnumber" placeholder="Serial Number" class="border px-3 py-2 rounded" required />
        <input type="password" name="access_token" placeholder="Access Token" class="border px-3 py-2 rounded" required />
        <%
          const today = new Date().toISOString().split('T')[0]; // yyyy-mm-dd
        %>
        <input type="date" name="from_date" class="border px-3 py-2 rounded" value="<%= typeof result !== 'undefined' && result?.fromDate ? result.fromDate : today %>" required />
        <input type="date" name="to_date" class="border px-3 py-2 rounded" value="<%= typeof result !== 'undefined' && result?.toDate ? result.toDate : today %>" required />
        

        <div class="flex items-center space-x-4">
          <label class="flex items-center space-x-2">
            <input type="radio" name="type" value="PDF" class="form-radio" checked />
            <span>PDF</span>
          </label>
          <label class="flex items-center space-x-2">
            <input type="radio" name="type" value="XML" class="form-radio" />
            <span>XML</span>
          </label>
        </div>

        <button type="submit" class="col-span-full bg-blue-500 text-white py-2 rounded hover:bg-blue-600">Thống kê</button>
      </form>

      <% if (typeof result !== 'undefined') { %>
        <% if (result && result.error) { %>
          <div class="bg-red-100 text-red-700 p-4 rounded shadow">
            <p><%= result.error %></p>
          </div>
        <% } else if (result) { %>
          <div class="bg-gradient-to-br from-green-100 to-green-200 p-8 rounded-2xl shadow-2xl w-full max-w-3xl mx-auto">
            <div class="flex items-center mb-4">
              <svg class="w-14 h-14 text-green-500 flex-shrink-0" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
                <circle cx="12" cy="12" r="10" stroke="currentColor" stroke-width="2" fill="white"/>
                <path stroke-linecap="round" stroke-linejoin="round" d="M8 12l2 2 4-4" />
              </svg>
              <h2 class="text-2xl font-bold text-green-700 ml-4">Kết quả thống kê</h2>
            </div>
            <div class="space-y-3 text-gray-700">
              <div class="flex flex-col md:flex-row md:items-center">
                <span class="font-semibold md:w-40">Serial:</span>
                <span class="break-words font-bold truncate md:flex-1"><%= result.serialnumber %></span>
              </div>
              <div class="flex flex-col md:flex-row md:items-center">
                <span class="font-semibold md:w-40">Loại:</span>
                <span class="break-words font-bold truncate md:flex-1"><%= result.type %></span>
              </div>
              <div class="flex flex-col md:flex-row md:items-center">
                <span class="font-semibold md:w-40">Số lượng ký:</span>
                <span class="text-green-700 font-bold text-3xl break-words truncate md:flex-1"><%= result.count %></span>
              </div>
            </div>
          </div>
        <% } else { %>
          <div class="text-gray-500 text-center italic">
            Không có kết quả để hiển thị.
          </div>
        <% } %>
      <% } %>

    </div>
  </main>

  <!-- Footer -->
  <%- include('../partials/footer') %>

</body>
</html>
