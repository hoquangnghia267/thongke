<style>
  :root {
    --primary-color: #ff9e43; /* Màu cam chính */
    --secondary-color: #ff9e43; /* Màu cam nhạt cho gradient */
    --text-color: #1f2937; /* Màu chữ chính (xám đậm) */
    --hover-color: #ff9e43; /* Màu khi hover link */
    --active-color: #ff9e43; /* Màu cho link đang active */
  }

  header {
    background: linear-gradient(to right, var(--primary-color), var(--secondary-color));
    transition: all 0.3s ease-in-out;
    z-index: 1000; /* Đảm bảo header ở trên sidebar */
  }

  .logo-svg {
    transition: transform 0.3s ease;
  }

  .logo-svg:hover {
    transform: rotate(360deg) scale(1.1);
  }

  nav a:hover {
    color: var(--hover-color);
  }

  .active-link {
    color: var(--active-color);
    font-weight: 600;
  }

  #mobile-menu {
    background: var(--secondary-color);
  }
</style>

<header class="px-6 py-4 flex items-center justify-between shadow-lg text-[var(--text-color)] animate-fade-in">
  <!-- Logo and Company Name -->
  <div class="flex items-center space-x-4">
    <img src="/images/logo1.png" alt="Logo" class="h-12 w-auto logo-svg" />
    <span class="text-xl md:text-2xl font-bold tracking-tight">
      CÔNG TY CỔ PHẦN CHỮ KÝ SỐ VI NA (SmartSign)
    </span>
  </div>

  <!-- Navigation and User Actions -->
  <% if (typeof user !== 'undefined') { %>
    <div class="flex items-center space-x-6">

      <a href="/logout" class="text-lg font-medium hover:text-red-500 transition-colors duration-200">Đăng xuất</a>

      <!-- Hamburger Menu for Mobile -->
      <button id="menu-toggle" class="md:hidden text-[var(--text-color)] focus:outline-none">
        <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16m-7 6h7"></path>
        </svg>
      </button>
    </div>

    <!-- Mobile Menu (Hidden by default) -->
    <div id="mobile-menu" class="hidden md:hidden w-full absolute top-16 left-0 shadow-lg">
      <nav class="flex flex-col items-center py-4 space-y-4 text-base font-medium">
        <a href="/logout" class="text-lg font-medium hover:text-red-500 transition-colors duration-200">Đăng xuất</a>
      </nav>
    </div>
  <% } %>
</header>

<!-- JavaScript for Hamburger Menu Toggle -->
<script>
  document.getElementById('menu-toggle')?.addEventListener('click', () => {
    const mobileMenu = document.getElementById('mobile-menu');
    mobileMenu.classList.toggle('hidden');
  });
</script>

<!-- Tailwind CSS CDN -->
<link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/animate.css/4.1.1/animate.min.css"/>